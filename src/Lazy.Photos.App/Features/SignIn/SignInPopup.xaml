<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
               xmlns:local="clr-namespace:Lazy.Photos.App"
               x:Class="Lazy.Photos.App.Features.SignIn.SignInPopup"
               CanBeDismissedByTappingOutsideOfPopup="True">

    <Grid BackgroundColor="#80000000"
          Padding="24">
        <Border Stroke="#00000000"
                BackgroundColor="#F2F3F8"
                StrokeShape="RoundRectangle 24"
                Padding="16"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                WidthRequest="320">
            <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto"
                  RowSpacing="14">
                <Grid ColumnDefinitions="Auto,*,Auto">
                    <Button FontFamily="MaterialDesignIcons"
                            Text="{x:Static local:IconFont.Close}"
                            FontSize="24"
                            BackgroundColor="Transparent"
                            TextColor="#222"
                            Command="{Binding CloseCommand}" />
                    <Label Grid.Column="1"
                           Text="Lazy Photos"
                           HorizontalTextAlignment="Center"
                           VerticalTextAlignment="Center"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="#111" />
                </Grid>

                <Button Grid.Row="1"
                        Text="Sign In"
                        BackgroundColor="#3F5A92"
                        TextColor="White"
                        CornerRadius="22"
                        HeightRequest="44"
                        Command="{Binding SignInCommand}" />

                <HorizontalStackLayout Grid.Row="2" Spacing="8">
                    <Label FontFamily="MaterialDesignIcons"
                           Text="{x:Static local:IconFont.Cog}"
                           FontSize="20"
                           TextColor="#222"
                           VerticalOptions="Center" />
                    <Button Text="Photos settings"
                            BackgroundColor="Transparent"
                            TextColor="#222"
                            Padding="0"
                            Command="{Binding OpenSettingsCommand}" />
                </HorizontalStackLayout>

                <HorizontalStackLayout Grid.Row="3" Spacing="8">
                    <Label FontFamily="MaterialDesignIcons"
                           Text="{x:Static local:IconFont.HelpCircle}"
                           FontSize="20"
                           TextColor="#222"
                           VerticalOptions="Center" />
                    <Button Text="Help &amp; feedback"
                            BackgroundColor="Transparent"
                            TextColor="#222"
                            Padding="0"
                            Command="{Binding OpenHelpCommand}" />
                </HorizontalStackLayout>

                <HorizontalStackLayout Grid.Row="4"
                                       Spacing="8"
                                       HorizontalOptions="Center">
                    <Button Text="Privacy Policy"
                            BackgroundColor="Transparent"
                            TextColor="#333"
                            Command="{Binding OpenPrivacyCommand}" />
                    <Label Text="|"
                           TextColor="#333"
                           VerticalTextAlignment="Center" />
                    <Button Text="Terms of Service"
                            BackgroundColor="Transparent"
                            TextColor="#333"
                            Command="{Binding OpenTermsCommand}" />
                </HorizontalStackLayout>
            </Grid>
        </Border>
    </Grid>

</toolkit:Popup>
