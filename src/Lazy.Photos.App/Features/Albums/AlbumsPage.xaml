<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Lazy.Photos.App"
             x:Class="Lazy.Photos.App.Features.Albums.AlbumsPage"
             Title="Albums"
             BackgroundColor="White">

    <Grid RowDefinitions="Auto,*">
        <HorizontalStackLayout Padding="16" Spacing="12">
            <Button FontFamily="MaterialDesignIcons"
                    Text="{x:Static local:IconFont.Plus}"
                    FontSize="24"
                    BackgroundColor="Transparent"
                    TextColor="#222"
                    Command="{Binding CreateAlbumCommand}" />
            <Label Text="Albums"
                   FontSize="24"
                   FontAttributes="Bold"
                   TextColor="#222"
                   VerticalOptions="Center" />
        </HorizontalStackLayout>

        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Albums}"
                        SelectionMode="Single"
                        SelectionChangedCommand="{Binding OpenAlbumCommand}"
                        SelectionChangedCommandParameter="{Binding Source={RelativeSource Self}, Path=SelectedItem}">
            <CollectionView.EmptyView>
                <VerticalStackLayout VerticalOptions="Center"
                                     HorizontalOptions="Center"
                                     Spacing="16"
                                     Padding="32">
                    <Label FontFamily="MaterialDesignIcons"
                           Text="{x:Static local:IconFont.FolderMultipleImage}"
                           FontSize="64"
                           TextColor="#999"
                           HorizontalOptions="Center" />
                    <Label Text="No albums yet"
                           FontSize="18"
                           TextColor="#666"
                           HorizontalOptions="Center" />
                    <Label Text="Create an album to organize your photos"
                           FontSize="14"
                           TextColor="#999"
                           HorizontalOptions="Center" />
                </VerticalStackLayout>
            </CollectionView.EmptyView>
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical"
                                 Span="2"
                                 HorizontalItemSpacing="8"
                                 VerticalItemSpacing="8" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Margin="8"
                            StrokeShape="RoundRectangle 12"
                            Stroke="#E0E0E0"
                            BackgroundColor="#F5F5F5"
                            HeightRequest="180">
                        <Grid RowDefinitions="*,Auto">
                            <Image Source="{Binding CoverThumbnail}"
                                   Aspect="AspectFill" />
                            <VerticalStackLayout Grid.Row="1"
                                                 Padding="12,8"
                                                 BackgroundColor="White">
                                <Label Text="{Binding Name}"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       TextColor="#222"
                                       LineBreakMode="TailTruncation" />
                                <Label Text="{Binding PhotoCount, StringFormat='{0} photos'}"
                                       FontSize="12"
                                       TextColor="#666" />
                            </VerticalStackLayout>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>

</ContentPage>
